# Portfolio Export

This folder contains a standalone static export of the AAQZ portfolio page.

Files:
- `index.html`
- `favicon.ico`
- `site-thumbs/*.jpg`

For GitHub Pages:
- push the contents of this folder to the root of the new repository, or
- keep this folder structure in a repo and publish from the `github/` directory if your workflow supports it.
